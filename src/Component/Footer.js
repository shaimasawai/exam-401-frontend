import React, { Component } from "react";
import Navbar from "react-bootstrap";

export class Footer extends Component {
  render() {
    return (
      <div>
        <Navbar>
          <Nav.Brand>Code Fellower</Nav.Brand>
        </Navbar>
      </div>
    );
  }
}

export default Footer;
